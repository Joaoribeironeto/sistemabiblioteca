COMANDOS DDL{

    CREATE TABLE estoque_biblioteca
    USE estoque_biblioteca
    CREATE TABLE livros (
    id INT AUTO_INCREMENT PRIMARY KEY,
    titulo VARCHAR(100) NOT NULL,
    autor VARCHAR(100) NOT NULL,
    isbn VARCHAR(20) UNIQUE NOT NULL,
    quantidade_de_paginas INT NOT NULL,
    edicao VARCHAR(50),
    editora VARCHAR(100)
);



}

